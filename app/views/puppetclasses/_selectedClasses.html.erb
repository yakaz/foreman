<%= content_tag_for :li, klass, :selected, :class => cycle('even', 'odd') + (klass.modeline.has_key?("internal-class") ? ' internal_puppetclass' : '') do %>
  <%= link_to_remove_puppetclass(klass) %>
  <%= link_to_edit_puppetclass_vars(klass, host) %>
  <%= smart_var_for(klass, type) %>
  <%= hidden_field_tag "#{type}[puppetclass_ids][]", klass.id %>
<% end -%>
